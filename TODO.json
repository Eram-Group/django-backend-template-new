{
  "version": 1,
  "plan": "PLAN.md",
  "created": "2026-07-11",
  "workflow": [
    "Groups run in array order; tasks run in array order within their group unless dependsOn says otherwise.",
    "baseline-commit blocks every other task (universal prerequisite; not repeated in each dependsOn).",
    "One task = one working session. Set status=in_progress before starting; set done only when the endGoal is observed, not assumed.",
    "Never start a task whose dependsOn are not all done.",
    "After the last task of a group: run the group's checkpoint, fix any fallout, then `git commit -m \"checkpoint(<groupId>): <group title>\"`.",
    "Tips are guidance, not prescriptions - decide from context; PLAN.md wins over any tip; record resolutions by appending to the task's tips.",
    "Any task that adds an Env field updates config/env.py AND .env.example in the same task.",
    "From G03 onward ruff+mypy must be green at every checkpoint; GitHub CI is expectedly broken until G10 rewrites it (no remote exists yet) - local checkpoints are the gate."
  ],
  "groups": [
    {
      "id": "G00",
      "title": "Baseline",
      "rationale": "Everything must be diffable against the untouched cookiecutter base; universal prerequisite.",
      "checkpoint": "git log --oneline shows exactly one commit; git status clean apart from gitignored files."
    },
    {
      "id": "G0C",
      "title": "Aggressive cleanup (delete old app + cruft)",
      "rationale": "User decision (2026-07-11): front-load the cleanup instead of deferring deletions to G03/G08 - delete everything that does not match PLAN.md, including sample_project itself. Accepted trade-off: the repo intentionally does NOT boot or run tests from here until G03 builds the new settings; G01/G02 do not need a booting app.",
      "checkpoint": "grep -ri sample_project (excluding .git/.venv/.idea and this file's own prose) returns nothing; uv sync clean + uv lock --check; docker compose -f docker-compose.local.yml up -d brings postgres:18 + mailpit healthy; pre-commit run --all-files green; commit."
    },
    {
      "id": "G01",
      "title": "Dependencies (add-only) + tool config",
      "rationale": "All code written from here on is measured against the final ruff/mypy/pytest rules.",
      "checkpoint": "uv sync clean; uv run ruff check + ruff format --check + uv run mypy (strict) all green with final config; commit."
    },
    {
      "id": "G02",
      "title": "Foundation code (built alongside, uninstalled)",
      "rationale": "New packages land while nothing imports them - zero-risk prep so the config-build group stays small.",
      "checkpoint": "uv run python -c 'import config.env, apps.common, apps.users' succeeds; commit."
    },
    {
      "id": "G03",
      "title": "Build the new config: settings + entrypoints (first boot)",
      "rationale": "The old app and settings are already gone (G0C), so this group builds the new config from scratch and produces the first bootable state of the new world.",
      "checkpoint": "docker compose -f docker-compose.local.yml up -d; drop old DB volume if present; uv run manage.py migrate + createcachetable; runserver: admin (env path), /healthz, /readyz, /api/v1/docs all respond; ruff+mypy green; commit."
    },
    {
      "id": "G04",
      "title": "Auth: passwordless headless + social",
      "rationale": "First vertical feature slice; needs env/settings/skeleton; Mailpit (old compose) makes the code flow verifiable end to end.",
      "checkpoint": "Request login code -> code visible in Mailpit -> confirm -> session works in browser mode and via X-Session-Token; GET /api/v1/users/me returns 200; unauthenticated returns 401 {\"message\"}; commit."
    },
    {
      "id": "G05",
      "title": "Tasks platform (background worker + scheduler)",
      "rationale": "Proves the db_worker path that the welcome email already uses AND the scheduler story: recurring jobs run as EventBridge Scheduler cron -> ECS run-task management commands (sample_scheduled_job is the template), while async work flows through the django.tasks DB queue. Small group with its own checkpoint because it validates infrastructure, not code volume.",
      "checkpoint": "Signup enqueues welcome email after commit; manage.py db_worker drains it to Mailpit; task result row visible in admin (or shell); sample_scheduled_job and the prune command run clean; commit."
    },
    {
      "id": "G06",
      "title": "Admin: unfold + framework",
      "rationale": "Needs the User model and auth to exist; lands before the test group so the admin-basics gate has a real admin to run against.",
      "checkpoint": "Admin themed by unfold; users admin fully works (list/search/filter/edit/export) with explicit can_* permissions; generate_dashboard scaffolds a valid package; superuser bootstrap is repeatable; commit."
    },
    {
      "id": "G07",
      "title": "Tests + static gates",
      "rationale": "Gates need everything they inspect (models, admin, APIs) to exist; from here the suite is the regression net protecting the infra groups.",
      "checkpoint": "uv run pytest green including both gates; coverage >= 80%; uv run lint-imports green; uv run mypy strict green repo-wide; commit."
    },
    {
      "id": "G08",
      "title": "Docker + compose rewrite",
      "rationale": "Image and compose need the final dependency set and commands (db_worker, healthz); the G07 suite catches regressions; wave-3 cruft (old compose) dies here.",
      "checkpoint": "docker compose up --build: web + worker + postgres:18 + mailpit all healthy; same auth/worker smokes pass against containers; image runs as non-root; commit."
    },
    {
      "id": "G09",
      "title": "Dev UX: justfile + pre-commit",
      "rationale": "Recipes reference compose.yaml/seed/worker/lint-imports, so it follows G07/G08; the pre-commit rewrite feeds the CI job next.",
      "checkpoint": "Every just recipe runs; pre-commit run --all-files green including pre-push hooks; commit."
    },
    {
      "id": "G10",
      "title": "CI/CD",
      "rationale": "CI mirrors pre-commit and the compose smoke, so it must come after both exist; ends the broken-remote-CI window.",
      "checkpoint": "Workflows lint clean (actionlint or careful review); after pushing to a GitHub remote: fast job + image job + secret scan green on a branch; commit."
    },
    {
      "id": "G11",
      "title": "Docs",
      "rationale": "Documenting last means documenting reality, not intent.",
      "checkpoint": "A newcomer can bootstrap from README alone; ARCHITECTURE.md matches the implemented tree; every Env field appears in .env.example; commit."
    },
    {
      "id": "G12",
      "title": "Final verification",
      "rationale": "PLAN.md's 7 verification steps as the closing sweep.",
      "checkpoint": "All three verify tasks done; final commit marks the transformation complete."
    }
  ],
  "tasks": [
    {
      "id": "baseline-commit",
      "group": "G00",
      "title": "Commit the untouched cookiecutter base",
      "endGoal": "One commit on main containing the unmodified base plus PLAN.md and TODO.json; git status clean apart from gitignored files (.env, .venv, .idea).",
      "tips": [
        "Verify .idea/ and .env are actually ignored by the cookiecutter .gitignore before committing.",
        ".envs/.local/* are committed by cookiecutter design (dev-only credentials) - leave as is; they die in G08.",
        "RESOLVED: .idea/ was not ignored by the generated .gitignore - appended .idea/ to .gitignore as part of the baseline commit.",
        "RESOLVED: this generation ignores .envs/* entirely (no !.envs/.local negation), so .envs files stay on disk untracked; old compose reads them from disk - unaffected. They still die in G08.",
        "RESOLVED: this task's commit doubles as the G00 checkpoint commit (checkpoint requires exactly one commit), so TODO.json status was set before committing."
      ],
      "status": "done"
    },
    {
      "id": "delete-mpa-cruft",
      "group": "G0C",
      "title": "Delete unreferenced cookiecutter cruft",
      "endGoal": "Gone: Sphinx docs/, .readthedocs.yml, docker-compose.docs.yml, docker-compose.production.yml, compose/ (entire tree), .devcontainer/, root tests/, merge_production_dotenvs_in_dotenv.py, CONTRIBUTORS.txt, .github/dependabot.yml, .envs/; locale/{en_US,fr_FR,pt_BR} + locale/README.md replaced by empty ar/ and en/ LC_MESSAGES skeletons; README.md reduced to a stub pointing at PLAN.md.",
      "tips": [
        "MOVED from G03 (was wave-1 deletion) per user decision to front-load cleanup; compose/ and .envs/ deletions absorbed from the old G08 wave-3 task.",
        ".envs/.production contains generated secrets - gitignored, so disk-only deletion; nothing enters history."
      ],
      "status": "done"
    },
    {
      "id": "cleanup-old-app",
      "group": "G0C",
      "title": "Delete sample_project + old config (settings, api, websocket)",
      "endGoal": "sample_project/, config/settings/, config/api.py, config/websocket.py deleted; manage.py/wsgi.py/asgi.py stripped of sys.path hacks and websocket dispatch; config/urls.py a minimal stub; docker-compose.local.yml rewritten to official postgres:18 (inline dev creds, healthcheck, /var/lib/postgresql/18/docker volume) + mailpit only; justfile trimmed to recipes that still work.",
      "tips": [
        "Repo intentionally does not boot from here until G03 builds the new settings - manage.py commands are expected to fail.",
        "G03's settings/urls tasks are now build-from-scratch (git history keeps the old files as reference)."
      ],
      "dependsOn": [
        "delete-mpa-cruft"
      ],
      "status": "done"
    },
    {
      "id": "deps-prune",
      "group": "G0C",
      "title": "Prune now-unused dependencies + dead tool config",
      "endGoal": "uv remove: collectfasta, crispy-bootstrap5, django-crispy-forms, django-environ, django-model-utils, django-redis, hiredis, pillow, python-slugify, redis, uvicorn, uvicorn-worker; dev group: django-coverage-plugin, djlint, sphinx, sphinx-autobuild, watchfiles; [project].name renamed sample_project -> backend; [tool.djlint] + [tool.pyproject-fmt] sections and their pre-commit hooks removed; coverage include points at apps/** + config/**; uv sync clean.",
      "tips": [
        "MOVED from G03 per user decision; the old app is gone so nothing imports the pruned packages.",
        "Survivors match PLAN's keep-list; allauth extras swap [mfa] -> [headless-spec,socialaccount] stays in G01 deps-add-runtime.",
        "RESOLVED (user decision 2026-07-11): dev-QoL tools are KEPT - ipdb, pytest-sugar, django-extensions, werkzeug[watchdog] (runserver_plus) stay in the dev group; watchfiles stays pruned (its only consumer was uvicorn's reloader). django_extensions must be added to local settings INSTALLED_APPS in G03 settings-envs.",
        "RESOLVED: also purged stale .gitignore entries (sample_project/media/, .envs/*, redis dump.rdb)."
      ],
      "dependsOn": [
        "cleanup-old-app"
      ],
      "status": "done"
    },
    {
      "id": "deps-add-runtime",
      "group": "G01",
      "title": "Add new runtime dependencies (no removals yet)",
      "endGoal": "uv sync clean with all PLAN step-2 runtime deps added and allauth extras switched [mfa] -> [headless-spec,socialaccount]; the old app still boots.",
      "tips": [
        "PLAN.md step 2 is the authoritative list (psycopg[binary,pool], pydantic-settings, dj-database-url, django-unfold, django-import-export+openpyxl, django-tasks-db, django-modeltranslation, django-structlog, django-guid, django-health-check, django-axes, django-ratelimit...).",
        "Verify whether Django 6 native django.tasks needs the django-tasks backport at all - likely only django-tasks-db (backend django_tasks_db.DatabaseBackend); resolve and append the answer here.",
        "RESOLVED (verified 2026-07-11): Django 6.0.7 ships native django.tasks; the only declared dep is django-tasks-db (0.12.0). The django-tasks backport arrives transitively as its internal dependency (the backend subclasses its base classes; compat.py accepts native django.tasks Task objects). Wiring: INSTALLED_APPS += django_tasks_db; TASKS = {default: {BACKEND: django_tasks_db.DatabaseBackend}}; worker = manage.py db_worker. Runtime smoke of native-enqueue -> db_worker happens in G05.",
        "Let uv pick versions (pin ranges, not exact patches)."
      ],
      "status": "done"
    },
    {
      "id": "deps-add-dev",
      "group": "G01",
      "title": "Add new dev-group dependencies",
      "endGoal": "pytest-xdist, pytest-cov, pytest-mock, django-test-migrations, import-linter in the dev group; lock updated; old-app pytest still green.",
      "status": "done",
      "tips": [
        "RESOLVED: 'old-app pytest still green' clause is moot post-G0C (no tests exist yet); observed: all five packages in dev group, lock updated, uv sync clean."
      ]
    },
    {
      "id": "toolcfg-rewrite",
      "group": "G01",
      "title": "Rewrite [tool.*] configs in pyproject.toml",
      "endGoal": "ruff (lean select + DTZ,T20,PT,S; S101 allowed in tests; isort force-single-line; migrations excluded), mypy strict + django-stubs + pydantic plugin, pytest, coverage (80% fail_under over apps/** and config/**) all match PLAN; djlint config removed.",
      "tips": [
        "The base already uses pytest 9's native [tool.pytest] table - keep it, do not revert to ini_options; keep --ds and add --reuse-db --import-mode=importlib (mind xdist interplay).",
        "mypy strict will scream about old-app code - acceptable until G03 deletes it; do not weaken the config to silence the doomed code.",
        "RESOLVED: a minimal config/settings/{__init__,test}.py stub was created so the django-stubs plugin can boot Django and mypy runs green from G01 onward (G03 rewrites it). mypy files= starts as [config, manage.py] - APPEND \"apps\" in G02. djlint config was already removed in G0C."
      ],
      "status": "done"
    },
    {
      "id": "env-module",
      "group": "G02",
      "title": "Create config/env.py (typed pydantic-settings Env)",
      "endGoal": "A typed Env(BaseSettings) importable with no .env present: SecretStr secrets, Literal environment, DB pool knobs, DJANGO_SUPERUSER_*, COOKIE_DOMAIN, FRONTEND_ALLOWED_ORIGINS, Google/Apple OAuth creds, safe local defaults, required-in-prod fields.",
      "tips": [
        "This is the ONLY reader of os.environ/.env in the codebase - settings must consume the env object exclusively.",
        "Gawdat template's config/helpers/env.py is the concept reference - modernize, don't port.",
        "RESOLVED: modernized from Gawdat config/helpers/env.py - key divergences: safe local defaults + required-in-prod model_validator (fail-fast lists ALL missing fields) instead of everything-required + dummy.env; env_ignore_empty=True (FOO= means unset); CSV lists via NoDecode+validator (no JSON in .env); no fetch-secrets-at-boot (anti-adopt, ECS injects)."
      ],
      "status": "done"
    },
    {
      "id": "env-example-file",
      "group": "G02",
      "title": "Author .env.example and local .env",
      "endGoal": ".env.example committed documenting every Env field (incl. ENVIRONMENT=local|staging|production) with safe local values; .env copied locally and gitignored.",
      "tips": [
        ".envs/ was deleted in G0C (local compose now uses inline dev creds); the new .env serves config/env.py only.",
        "RESOLVED: .env.example groups fields with comments; .env copied locally (gitignored, verified). Empty-value semantics: FOO= means unset."
      ],
      "status": "done"
    },
    {
      "id": "common-core",
      "group": "G02",
      "title": "Create apps/common core (BaseModel, errors, pagination)",
      "endGoal": "apps/common importable standalone: BaseModel with UUIDv7 pk (db_default=Func(function=\"uuidv7\")), indexed created_at, updated_at; ApplicationError hierarchy; cursor pagination ordered by pk; apps.py.",
      "tips": [
        "uuidv7() is PG18-native - db_default keeps generation in the database.",
        "Old app untouched; nothing imports apps.common yet.",
        "RESOLVED: ApplicationError designed fresh (single base with message+extra; Gawdat had per-app APIException + ErrorCode objects - not adopted). CursorPagination orders -pk (uuid7 = newest first), opaque urlsafe-b64 cursor, limit<=100. apps/common/__init__ stays EMPTY: leaf modules import ninja/django lazily-by-consumer so bare 'import apps.common' works without settings."
      ],
      "status": "done"
    },
    {
      "id": "common-health",
      "group": "G02",
      "title": "Health endpoints on django-health-check",
      "endGoal": "apps/common/health.py exposes /healthz (liveness, no DB) and /readyz (DB/cache/storage) view functions, importable; URL wiring lands with the flip.",
      "status": "done",
      "tips": [
        "RESOLVED: healthz = plain 200 no-DB; readyz = CheckMixin().check_all() over registered plugins, 503 + failed dict. health_check import kept inside view; mypy override for health_check.* (no py.typed)."
      ]
    },
    {
      "id": "users-core",
      "group": "G02",
      "title": "Create apps/users core (model, manager, adapters skeleton)",
      "endGoal": "apps/users importable: User(BaseModel, AbstractUser-derived) with email login, no username, single name field, language (ar/en); email-based manager; constants.py, exceptions.py; minimal adapters.py with is_open_for_signup from env. No migrations generated yet.",
      "tips": [
        "Keep the default app label 'users' so AUTH_USER_MODEL = \"users.User\" never changes - but never have old and new users apps installed simultaneously.",
        "Do NOT run makemigrations here - fresh 0001 is generated after the flip (users-migrations-fresh).",
        "RESOLVED (user decisions 2026-07-11): language defaults to ARABIC; full exemplar tree created now - placeholder layer packages (schemas/services/selectors/apis/tasks/admin/management/tests) carry docstrings naming their fill group. Manager: password=None -> set_unusable_password (passwordless default). Adapter lives IN the app (fixes Gawdat placement smell) with mypy disallow_subclassing_any=false override for apps.*.adapters. Settings stub extended with INSTALLED_APPS+AUTH_USER_MODEL so mypy strict stays green; mypy files now include apps."
      ],
      "status": "done"
    },
    {
      "id": "settings-base",
      "group": "G03",
      "title": "Rewrite config/settings/base.py on the typed env",
      "endGoal": "manage.py check passes with new base settings: apps.* installed, old sample_project apps gone, dj-database-url DATABASES + ATOMIC_REQUESTS, DatabaseCache + LocMem 'ratelimit' alias, TASKS db backend, DB sessions, Argon2 hashers, LANGUAGES ar/en + UTC + LOCALE_PATHS, structlog, CSP, EMAIL_TIMEOUT=5, axes, CORS + CSRF_TRUSTED_ORIGINS from env, minimal allauth block pointing at apps.users.adapters.",
      "tips": [
        "INSTALLED_APPS order: modeltranslation AND unfold (+unfold.contrib.import_export) MUST precede django.contrib.admin; import_export after unfold's contrib - wrong order fails silently.",
        "Middleware order per PLAN: guid -> security -> ... -> session -> locale -> ... -> axes (axes last).",
        "Drop the old ACCOUNT_FORMS/SOCIALACCOUNT_FORMS string refs to sample_project.users.forms.",
        "django.contrib.sites leaves too: remove SITE_ID and the MIGRATION_MODULES sites override; verify allauth 65.x boots without sites."
      ],
      "dependsOn": [
        "deps-add-runtime",
        "env-module",
        "common-core",
        "users-core"
      ],
      "status": "pending"
    },
    {
      "id": "settings-envs",
      "group": "G03",
      "title": "Rewrite local.py, production.py, test.py",
      "endGoal": "All four settings modules import cleanly: local (DEBUG, Mailpit SMTP, debug toolbar + Docker INTERNAL_IPS trick, django_extensions installed, ImmediateBackend toggle), production (security block, __Secure- cookie names, psycopg pool from env + CONN_MAX_AGE=0, S3 manifest static + S3 media, SES via Anymail, Sentry, JSON logs, DisallowedHost logger silenced), test (fast hashers, ImmediateBackend, locmem email AND locmem cache, no-migrations option).",
      "tips": [
        "Pool OPTIONS pair with CONN_MAX_AGE=0 (production only) and NO CONN_HEALTH_CHECKS; local/test use plain connections.",
        "__Secure- cookie names are production-only (they require the Secure flag and break http://localhost).",
        "CSRF_COOKIE_HTTPONLY=False is deliberate - the SPA must read the CSRF cookie (PLAN Notes).",
        "createcachetable is NOT run by migrate - test settings must use LocMem cache or the suite fails on first cache hit."
      ],
      "status": "pending"
    },
    {
      "id": "config-api-v1",
      "group": "G03",
      "title": "Create config/api/v1.py + exception handlers",
      "endGoal": "/api/v1/docs renders (staff-gated, open in local via env/DEBUG) with zero routers mounted; config/api/exception_handlers.py maps ApplicationError, validation errors and 404 to {\"message\": ..., \"extra\": {\"fields\": ...}}.",
      "tips": [
        "Port the cookiecutter openapi-tests idea later in G07 (docs 200 for staff, anon redirected)."
      ],
      "status": "pending"
    },
    {
      "id": "urls-entrypoints",
      "group": "G03",
      "title": "Build config/urls.py (entrypoints already cleaned in G0C)",
      "endGoal": "URL conf resolves: admin at env path, /api/v1/, /healthz + /readyz, allauth headless mounted; manage.py defaults to config.settings.local (verify - set in G0C).",
      "tips": [
        "websocket.py, old api.py, and the sys.path hacks were already removed in G0C cleanup-old-app; urls.py is currently a stub."
      ],
      "status": "pending"
    },
    {
      "id": "users-migrations-fresh",
      "group": "G03",
      "title": "Generate fresh initial migration for apps.users",
      "endGoal": "apps/users/migrations/0001_initial.py generated under the flipped settings; migrate succeeds on a fresh database; makemigrations --check clean.",
      "tips": [
        "Never write an alter-pk migration - UUIDv7 pk is incompatible with the old AutoField rows; there is no production data, so drop the local DB volume and start clean."
      ],
      "dependsOn": [
        "settings-base"
      ],
      "status": "pending"
    },
    {
      "id": "allauth-headless-settings",
      "group": "G04",
      "title": "Full passwordless allauth headless configuration",
      "endGoal": "Headless auth endpoints mounted and listed in the served auth OpenAPI spec: HEADLESS_ONLY=True, HEADLESS_FRONTEND_URLS, ACCOUNT_LOGIN_BY_CODE_REQUIRED, ACCOUNT_SIGNUP_FIELDS=[\"email*\"], email verification by code, allauth.usersessions installed, browser + app client types active.",
      "tips": [
        "No password fields anywhere in signup; staff/superusers still keep passwords for admin login (Argon2)."
      ],
      "status": "pending"
    },
    {
      "id": "adapters-passwordless",
      "group": "G04",
      "title": "Complete the passwordless adapter",
      "endGoal": "Requesting a login code delivers a 6-digit numeric code to Mailpit; OTP is skipped for social logins; ACCOUNT_ALLOW_REGISTRATION env kill-switch works (signup rejected when off).",
      "tips": [
        "Verify the exact allauth 65.x adapter hooks (generate_login_code / generate_email_verification_code / is_login_by_code_required) before writing - the Gawdat adapter is the concept reference."
      ],
      "status": "pending"
    },
    {
      "id": "social-providers",
      "group": "G04",
      "title": "Google + Apple social login from env",
      "endGoal": "Settings-based SOCIALACCOUNT_PROVIDERS built from env creds with verified-email auto-connect; absent creds degrade gracefully in local (no crash, provider simply unavailable).",
      "tips": [
        "Apple's secret is a multiline PEM - pass it base64-encoded through a SecretStr env field and decode in settings."
      ],
      "status": "pending"
    },
    {
      "id": "ninja-auth-class",
      "group": "G04",
      "title": "Ninja auth class (session cookie or X-Session-Token)",
      "endGoal": "Set as the NinjaAPI default auth: valid session cookie OR valid allauth app-client X-Session-Token authenticates; unauthenticated call returns 401 with the {\"message\"} error shape.",
      "dependsOn": [
        "config-api-v1"
      ],
      "status": "pending"
    },
    {
      "id": "users-me-api",
      "group": "G04",
      "title": "Users /me vertical slice (schemas, selectors, services, apis)",
      "endGoal": "GET and PATCH /api/v1/users/me work end to end through the layered path: schemas (UserSummary, UserDetail(Summary), UserUpdateIn all-optional PATCH via exclude_unset), selector fetches only what it serializes, service validates with full_clean before save, thin router mounted in v1.",
      "tips": [
        "This is THE exemplar app - every future app copies its shape; no business logic in the router.",
        "No signup route here - signup belongs to allauth.",
        "CODE WRITTEN EARLY (2026-07-11, user request to fill apps/users): statically verified (ruff+mypy strict green; app-registry boot + layer imports + router mount inspected) but the endGoal is RUNTIME-verified - do that at this group's checkpoint before marking done.",
        "Implemented: schemas/users.py (UserSummary/Detail/UpdateIn), selectors/users.py (user_get/user_list), services/users.py (user_update with USER_UPDATABLE_FIELDS whitelist + full_clean), apis/users.py (/me GET+PATCH via AuthedRequest typing) aggregated by apis/router.py."
      ],
      "status": "pending"
    },
    {
      "id": "signup-signal-welcome-task",
      "group": "G04",
      "title": "Signup signal -> service -> welcome-email task",
      "endGoal": "user_signed_up receiver in apps/users/signals.py (wired in apps.py ready()) calls services.user_post_signup, which enqueues tasks/emails.send_welcome_email via transaction.on_commit; the email renders in user.language; a queued task row exists after signup.",
      "tips": [
        "Receiver stays thin - one call into the service; the import-linter layer contract includes signals in the top layer.",
        "CODE WRITTEN EARLY (2026-07-11, user request to fill apps/users): statically verified (ruff+mypy strict green; app-registry boot + layer imports + router mount inspected) but the endGoal is RUNTIME-verified - do that at this group's checkpoint before marking done.",
        "Implemented: signals.py user_signed_up receiver -> services.user_post_signup -> transaction.on_commit enqueue of tasks/emails.send_welcome_email (renders in user.language via translation.override); wired in apps.py ready(); receiver connection verified at setup()."
      ],
      "status": "pending"
    },
    {
      "id": "worker-e2e",
      "group": "G05",
      "title": "Run the DB worker end to end",
      "endGoal": "manage.py db_worker drains the queued welcome email to Mailpit and records a successful task result; the worker runbook (command, graceful SIGTERM, ECS stopTimeout note) is written down for README.",
      "dependsOn": [
        "signup-signal-welcome-task"
      ],
      "status": "pending"
    },
    {
      "id": "ops-commands",
      "group": "G05",
      "title": "Scheduled-job template + task-result pruning",
      "endGoal": "apps/users/management/commands/sample_scheduled_job.py (thin wrapper over a service) runs clean; a prune path for old task results exists and runs; the EventBridge cron recipe (clearsessions + prune + sample) is noted for README.",
      "tips": [
        "Check whether django-tasks-db ships a prune management command before writing one.",
        "RESOLVED (verified 2026-07-11 from django-tasks-db 0.12.0): it ships prune_db_task_results with retention-policy options - do not write one. Bonus: the backend supports OPTIONS id_function (incl. db expressions on Django 6) if we want uuidv7 task ids.",
        "CODE WRITTEN EARLY (2026-07-11, user request to fill apps/users): statically verified (ruff+mypy strict green; app-registry boot + layer imports + router mount inspected) but the endGoal is RUNTIME-verified - do that at this group's checkpoint before marking done.",
        "Implemented: management/commands/sample_scheduled_job.py (thin wrapper over selectors). Prune command NOT needed (django-tasks-db ships prune_db_task_results).",
        "This platform is the SCHEDULER too: recurring jobs = EventBridge Scheduler cron -> ECS run-task invoking management commands (no in-process beat); one-off/async work = django.tasks queue drained by db_worker."
      ],
      "status": "pending"
    },
    {
      "id": "unfold-config",
      "group": "G06",
      "title": "Unfold configuration (theme + sidebar)",
      "endGoal": "Admin is unfold-themed on every default page; UNFOLD dict has site title and sidebar navigation (users, task results) with all links built via reverse; optional env-gated secure_admin_login documented but off by default.",
      "status": "pending"
    },
    {
      "id": "admin-framework",
      "group": "G06",
      "title": "BaseModelAdmin framework in apps/common/admin/",
      "endGoal": "Clean re-implementation importable and strict-mypy green: unfold-based BaseModelAdmin forcing explicit can_add/can_change/can_delete, FieldPermissions + AdminContext for field-level visibility/editability, base Tabular/Stacked inlines (hidden on add view), import-export resource base.",
      "tips": [
        "Concepts from the Gawdat template, none of its legacy code: no compat aliases, no deprecated helpers, no metaclass gymnastics beyond what ABC genuinely requires.",
        "Multi-file skeleton created early (user request 2026-07-11): apps/common/admin/ {model_admin, field_permissions, context, inlines, resources}.py - fill these, then re-export the public interface from __init__."
      ],
      "status": "pending"
    },
    {
      "id": "generate-dashboard-cmd",
      "group": "G06",
      "title": "generate_dashboard scaffolder command",
      "endGoal": "manage.py generate_dashboard <app> <Model> emits a valid admin/<entity>/ package (admin, list_view, change_view, display, permissions, resource + CHECKLIST.md) that imports clean against the framework.",
      "status": "pending",
      "tips": [
        "The stub admin/user/ files already exist - the scaffolder needs an --overwrite/skip strategy for pre-existing files."
      ]
    },
    {
      "id": "users-admin-package",
      "group": "G06",
      "title": "Users admin package (dogfood the scaffolder)",
      "endGoal": "admin/user/ generated then fleshed out: search, filters, readonly rules, export resource, explicit can_* decisions; User fully manageable in admin.",
      "dependsOn": [
        "admin-framework",
        "generate-dashboard-cmd"
      ],
      "status": "pending",
      "tips": [
        "Multi-file skeleton created early (user request 2026-07-11): apps/users/admin/user/ {admin, list_view, change_view, display, permissions, resource}.py + CHECKLIST.md - generate_dashboard must tolerate/overwrite existing stub files."
      ]
    },
    {
      "id": "superuser-bootstrap",
      "group": "G06",
      "title": "Idempotent superuser bootstrap",
      "endGoal": "A repeatable flow (native createsuperuser --noinput reading DJANGO_SUPERUSER_* env, wrapped idempotently in just) yields a working staff login on a fresh database.",
      "status": "pending"
    },
    {
      "id": "conftest-root",
      "group": "G07",
      "title": "Root conftest + pytest plumbing",
      "endGoal": "Root conftest.py provides autouse tmp MEDIA_ROOT and a user fixture; a trivial test passes under pytest -n auto with coverage wiring active.",
      "status": "pending"
    },
    {
      "id": "factories-registry",
      "group": "G07",
      "title": "Factories + explicit registry",
      "endGoal": "apps/users/tests/factories.py (email get-or-create, skip_postgeneration_save) plus an explicit per-app factory registry in apps/common/tests/ that fails loudly on unregistered entries - no auto-discovery, no swallowed exceptions.",
      "status": "pending",
      "tips": [
        "CODE WRITTEN EARLY (2026-07-11, user request to fill apps/users): statically verified (ruff+mypy strict green; app-registry boot + layer imports + router mount inspected) but the endGoal is RUNTIME-verified - do that at this group's checkpoint before marking done.",
        "UserFactory written early in apps/users/tests/factories.py (email get-or-create, skip_postgeneration_save, DjangoModelFactory[User] - runtime subscript verified). The registry itself (apps/common/tests/) is still G07 work.",
        "USER INVESTIGATION PENDING (2026-07-11): Omar wants to investigate the factories topic himself - factory_boy (DjangoModelFactory, Sequence, django_get_or_create, skip_postgeneration_save) and Faker providers - before this is finalized. Treat apps/users/tests/factories.py as a DRAFT; do not implement the registry or mark this done without his review."
      ]
    },
    {
      "id": "factory-coverage-gate",
      "group": "G07",
      "title": "Factory-coverage gate",
      "endGoal": "A test iterating apps.get_models() fails listing any concrete model without a registered factory (third-party/historical models excluded); passes on the real tree.",
      "dependsOn": [
        "factories-registry"
      ],
      "status": "pending",
      "tips": [
        "Blocked on the factories investigation hold recorded in factories-registry."
      ]
    },
    {
      "id": "admin-basics-gate",
      "group": "G07",
      "title": "Admin basics gate (registry-driven, zero per-admin tests)",
      "endGoal": "The generic suite from PLAN's 'Admin basics gate' section passes in apps/common/tests/admin/: index/changelists, search, every filter applied, per-column sorting both directions, add/change/delete/history pages, unchanged-save round-trip, export CSV with rows, autocomplete endpoints, data coverage, unfold sidebar links resolve - permission-aware, session-auto-seeded, raise_request_exception=True.",
      "tips": [
        "The save round-trip must GET the form and re-POST it unchanged (the legacy version that only POSTs _continue passes on validation failures)."
      ],
      "dependsOn": [
        "factories-registry",
        "users-admin-package"
      ],
      "status": "pending"
    },
    {
      "id": "users-tests",
      "group": "G07",
      "title": "Users app tests (services/selectors/apis + auth flow)",
      "endGoal": "Meaningful tests for the exemplar app: service validation paths, selector shapes, /me API round-trips, the headless code-login flow (request code -> confirm -> session), adapter behavior (kill-switch, 6-digit codes), and the openapi docs gating (staff 200, anon redirected).",
      "status": "pending"
    },
    {
      "id": "seed-db-cmd",
      "group": "G07",
      "title": "seed_db command over the factory registry",
      "endGoal": "manage.py seed_db populates a local database through the registry (guarded against production); seeded data is browsable in admin.",
      "dependsOn": [
        "factories-registry"
      ],
      "status": "pending"
    },
    {
      "id": "import-linter-contracts",
      "group": "G07",
      "title": "Import-linter contracts enforced",
      "endGoal": "[tool.importlinter] contracts from PLAN (layers incl. signals, domain-app independence, common-imports-no-domain-app) pass via lint-imports; suite green; coverage >= 80%; mypy strict green repo-wide.",
      "dependsOn": [
        "delete-mpa-cruft"
      ],
      "status": "pending"
    },
    {
      "id": "dockerfile-multistage",
      "group": "G08",
      "title": "Rewrite the production Dockerfile",
      "endGoal": "docker build succeeds from the pruned lock: uv builder image -> python:3.14-slim runtime, two-phase uv sync --locked --no-dev with BuildKit cache mounts + bind-mounted lock/pyproject, UV_COMPILE_BYTECODE=1 + UV_PYTHON_DOWNLOADS=0, compilemessages baked in (gettext in build stage), non-root user, CMD gunicorn honoring WEB_CONCURRENCY with --max-requests + jitter; NO collectstatic at build or boot.",
      "tips": [
        "The image is the single ECS artifact - the worker service overrides the command (manage.py db_worker).",
        "Release step (migrate + createcachetable + collectstatic) is a one-off ECS task, never the entrypoint."
      ],
      "dependsOn": [
        "deps-prune"
      ],
      "status": "pending"
    },
    {
      "id": "dockerignore-rewrite",
      "group": "G08",
      "title": "Rewrite .dockerignore",
      "endGoal": "Build context is minimal: .git, .venv, caches, tests artifacts, .env*, docs excluded; image content unchanged by stray local files.",
      "status": "pending"
    },
    {
      "id": "compose-rewrite",
      "group": "G08",
      "title": "New root compose.yaml (local dev + CI smoke)",
      "endGoal": "docker compose up --build brings up web (develop: watch:, .env), worker (same image, db_worker command), official postgres:18 (healthchecked, correct volume), mailpit; compose sets DATABASE_URL/DJANGO_SETTINGS_MODULE explicitly so .env stays host-oriented.",
      "tips": [
        "postgres:18 moved its data dir - mount /var/lib/postgresql/18/docker or persistence silently breaks.",
        "depends_on with condition: service_healthy replaces wait-for-it scripts."
      ],
      "dependsOn": [
        "dockerfile-multistage"
      ],
      "status": "pending"
    },
    {
      "id": "delete-docker-cruft",
      "group": "G08",
      "title": "Wave-3 deletion: old local compose file",
      "endGoal": "docker-compose.local.yml deleted once the new compose.yaml replaces it (compose/ and .envs/ already died in G0C); a single Dockerfile + compose.yaml remain.",
      "dependsOn": [
        "compose-rewrite"
      ],
      "status": "pending"
    },
    {
      "id": "justfile-rewrite",
      "group": "G09",
      "title": "Rewrite the justfile",
      "endGoal": "Full recipe set runs: bootstrap (uv sync + .env copy + pre-commit install incl. pre-push + migrate + createcachetable), up/stop/logs/manage passthrough, test, lint, fmt, typecheck, migrate, makemigrations, shell, worker, seed, db-reset (warned), bash, clean, check-deploy.",
      "dependsOn": [
        "compose-rewrite",
        "seed-db-cmd"
      ],
      "status": "pending"
    },
    {
      "id": "precommit-rewrite",
      "group": "G09",
      "title": "Rewrite .pre-commit-config.yaml",
      "endGoal": "pre-commit run --all-files green with: ruff-check/ruff-format (official mirror), django-upgrade --target-version 6.0, gitleaks, uv lock --check, hygiene hooks (trailing-whitespace, end-of-file, check-yaml/toml/json, check-added-large-files, merge-conflict, debug-statements, detect-private-key), lint-imports; pre-push stage runs branch-behind-main check + makemigrations --check; djlint and pyproject-fmt hooks removed (done early in G0C).",
      "tips": [
        "The exclude: line still references deleted paths (^docs/, devcontainer.json) - set the final exclude (migrations only) in this rewrite."
      ],
      "dependsOn": [
        "import-linter-contracts",
        "toolcfg-rewrite"
      ],
      "status": "pending"
    },
    {
      "id": "ci-workflow-rewrite",
      "group": "G10",
      "title": "Rewrite ci.yml as the hybrid pipeline",
      "endGoal": "Two parallel jobs with concurrency cancel-in-progress: fast job (setup-uv -> pre-commit action -> mypy -> uv lock --check -> makemigrations --check -> pytest against a postgres:18 service incl. createcachetable) and image job (buildx with GHA layer cache -> compose up -> smoke /healthz, /readyz, one API call).",
      "dependsOn": [
        "precommit-rewrite",
        "compose-rewrite"
      ],
      "status": "pending"
    },
    {
      "id": "secret-scan-workflow",
      "group": "G10",
      "title": "Secret-scan workflow",
      "endGoal": "TruffleHog full-history scan (fetch-depth: 0) runs on push/PR as its own workflow.",
      "tips": [
        "History is clean from the baseline commit onward - keep it that way; the scan is unforgiving once pushed."
      ],
      "status": "pending"
    },
    {
      "id": "ecr-push-workflow",
      "group": "G10",
      "title": "Main-branch ECR push via OIDC",
      "endGoal": "A main-only workflow builds and pushes the image tagged github.sha to ECR assuming a role via GitHub OIDC (no static AWS keys); skips gracefully when AWS repo config is absent; documented as the deploy artifact.",
      "dependsOn": [
        "dockerfile-multistage"
      ],
      "status": "pending"
    },
    {
      "id": "renovate-config",
      "group": "G10",
      "title": "Renovate replaces Dependabot",
      "endGoal": "renovate.json validates (uv lockfile, github-actions and docker managers, sensible grouping); .github/dependabot.yml was already deleted in G0C.",
      "status": "pending"
    },
    {
      "id": "readme-rewrite",
      "group": "G11",
      "title": "Rewrite README",
      "endGoal": "A newcomer can go from clone to running stack using README alone: quickstart, command table, AWS runbook (two ECS services from one image, release task = migrate + createcachetable + collectstatic, RDS PG18, S3+CloudFront, Secrets Manager injection, staging/production env vars, EventBridge cron recipe).",
      "status": "pending"
    },
    {
      "id": "architecture-doc",
      "group": "G11",
      "title": "Write docs/ARCHITECTURE.md",
      "endGoal": "The layering rules, error contract, schema/pagination conventions, task/command conventions (idempotency, on_commit, retry), translation.py pattern, admin framework usage, and the deferred/swap paths from PLAN Notes - matching implemented reality, not intent.",
      "status": "pending"
    },
    {
      "id": "claude-md-env-audit",
      "group": "G11",
      "title": "CLAUDE.md + final .env.example audit",
      "endGoal": "Repo CLAUDE.md digests the architecture rules and daily commands for AI-assisted work; .env.example verified field-by-field against config/env.py (nothing missing, nothing stale).",
      "status": "pending"
    },
    {
      "id": "verify-static-suite",
      "group": "G12",
      "title": "Static verification sweep (PLAN verification 1, 4, 5)",
      "endGoal": "uv sync clean; manage.py check OK; makemigrations --check clean; pytest green incl. both gates with coverage >= 80%; ruff check, ruff format --check, mypy, lint-imports, pre-commit run --all-files all green in one sitting.",
      "status": "pending"
    },
    {
      "id": "verify-runtime-flows",
      "group": "G12",
      "title": "Runtime flow verification (PLAN verification 2, 3)",
      "endGoal": "Fresh DB -> release steps -> runserver: /api/v1/docs, /healthz, /readyz, admin all OK; full signup/login-by-code flow in both client modes; welcome email through db_worker into Mailpit; task row in admin; sample_scheduled_job runs; 401 error shape; 429 on ratelimit burst - every step observed, none assumed.",
      "status": "pending"
    },
    {
      "id": "verify-prod-posture",
      "group": "G12",
      "title": "Production posture verification (PLAN verification 6, 7)",
      "endGoal": "check --deploy with production settings reports zero warnings; docker compose up --build passes the same smokes with a non-root container; final commit marks the transformation complete.",
      "status": "pending"
    }
  ]
}
