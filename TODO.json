{
  "version": 1,
  "plan": "PLAN.md",
  "created": "2026-07-11",
  "workflow": [
    "Groups run in array order; tasks run in array order within their group unless dependsOn says otherwise.",
    "Completed work is removed from this file - done through the easy-sweep (2026-07-11): G00-G05, G08, G09, G10 (workflows verified green on GitHub 2026-07-18: remote created at omargawdat/django-backend-template, first real run exposed 5 defects invisible locally - unresolvable setup-uv@v8 tag, invalid `compose run --no-build` flag, TruffleHog flagging the repo's own dev creds, guard-migrations missing the pull-requests scope so it failed every PR without ever evaluating its rule, and a .env.example trailing comment parsed as the value; all fixed in PR #1, and the migration guard was then proven to reject a modified migration and to bypass on the allow-migration-edit label), superuser bootstrap, and the /users/me slice; G14 template-enhancement sweep fully adopted 2026-07-12 (wp1-wp8 commits); G15 external-client build complete 2026-07-12 (wp-a HTTP kernel, wp-b User.phone, wp-c apps/notifications, wp-d apps/payments, wp-e docs - plan in ~/.claude/plans/toasty-snuggling-gosling.md). Group ARRAY ORDER = execution order.",
    "2026-07-18 regroup (user request): remaining work reorganized into thematic groups G16-G20 (authentication / testing incl. factories / cron jobs / AWS instructions / Claude structure). Empty shells G07, G11, G12 removed - their tasks were already swept; G07's gate requirements live on in G17's checkpoint. G13's deferred-review intent (reviews don't block build work) carries on in the review tasks' tips unchanged.",
    "One task = one working session. Set status=in_progress before starting; set done only when the endGoal is observed, not assumed.",
    "Never start a task whose dependsOn are not all done.",
    "After the last task of a group: run the group's checkpoint, fix any fallout, then `git commit -m \"checkpoint(<groupId>): <group title>\"`.",
    "Tips are guidance, not prescriptions - decide from context; PLAN.md wins over any tip; record resolutions by appending to the task's tips.",
    "Any task that adds an Env field updates config/env.py AND .env.example in the same task.",
    "From G03 onward ruff+mypy must be green at every checkpoint. Since 2026-07-18 the remote exists and CI runs for real, so GitHub is a gate alongside the local checkpoint - not a substitute: CI is the only place `cp .env.example .env` happens, which is exactly how the FIREBASE_CREDENTIALS_B64 comment-as-value bug surfaced.",
    "2026-07-18 saved cards built (user request; plan in ~/.claude/plans/curious-bubbling-kernighan.md): SavedCard vault + opt-in save at checkout + one-click CIT + service-only MIT (payment_charge_saved) + cards API + admin, Tap tokens->charge with payment_agreement, Paymob TOKEN callback (own 8-field HMAC) + card_tokens CIT + MOTO payments/pay; local suites green - the live test-mode proof is G22. Simplified later the same day (user request): save is always-on (save_card left the API), and the add-card-without-payment flow (Tap authorize+auto-void, Paymob Verification intention - both live-verified) was removed; git history keeps the working implementation."
  ],
  "groups": [
    {
      "id": "G16",
      "title": "Authentication",
      "rationale": "Everything auth-shaped in one place: build social login when external creds exist, then the user-led deep review signs off the whole implemented stack (social included, so social-providers runs first).",
      "checkpoint": "Social login works with real Google/Apple creds (or documented graceful degrade when absent); auth review signed off with decisions recorded in tips; ruff+mypy+pytest+lint-imports green; commit."
    },
    {
      "id": "G17",
      "title": "Testing & factories",
      "rationale": "The test suite and its data layer reviewed together: factory_boy structure, mimesis values, registry coverage gate, seed pipeline - plus end-to-end proof that the external-gateway flows (payments, SMS) actually work, not just pass unit tests.",
      "checkpoint": "Factories review signed off; payment + SMS end-to-end flows demonstrated and evidence recorded in the task's tips; uv run pytest green including the factory-coverage and cross-app gates; coverage >= 80%; commit."
    },
    {
      "id": "G18",
      "title": "Cron & scheduled jobs",
      "rationale": "django.tasks (Django 6.0) ships no worker, no retries, no delayed execution, no periodic tasks - the project must own each gap explicitly: deadline-as-data + idempotent sweeps for delay/retry, an external cadence trigger for recurrence, pruning for result rows.",
      "checkpoint": "Sweep pattern demonstrated end-to-end locally (enqueue -> worker executes -> re-run is a no-op); every gap in the task tips has a named owner (code, worker, or scheduler); docs updated; gates green; commit."
    },
    {
      "id": "G19",
      "title": "AWS instructions",
      "rationale": "Deployment knowledge from the ECS Express Mode research (Nov 2025 launch through July 2026 updates) written down before it goes stale; the web tier, worker, and schedules each need a stated home.",
      "checkpoint": "A deployment runbook exists in docs/ that a newcomer could follow to stand up web + worker + schedules from scratch; commit."
    },
    {
      "id": "G20",
      "title": "Claude structure",
      "rationale": "CLAUDE.md digest, .claude/rules topic files, PLAN.md, and ARCHITECTURE.md must say the same thing - drift here silently misleads every future session.",
      "checkpoint": "Digest is a faithful summary; every digest rule that has depth has a .claude/rules file that auto-loads on the right globs; no contradictions across CLAUDE.md, PLAN.md, ARCHITECTURE.md; commit."
    },
    {
      "id": "G21",
      "title": "Product expansion",
      "rationale": "Market-facing features requested 2026-07-18: true multi-country/multi-currency support beyond the SAR/EGP pair, and operational zones managed from the dashboard instead of code.",
      "checkpoint": "Each feature demonstrated end-to-end locally; design decisions recorded in the task tips; ruff+mypy+pytest+lint-imports green; commit."
    },
    {
      "id": "G22",
      "title": "Saved cards live verification",
      "rationale": "Card-on-file was built and unit/service-tested against respx + FakeGateway on 2026-07-18; both provider accounts have the features enabled. What code cannot prove locally: real 3DS on the token charge, the real TOKEN callback shape/HMAC, Card-on-File/MOTO integration id behavior, and Tap's card+agreement payload on a live save.",
      "checkpoint": "Saved-card save/one-click/MIT/delete demonstrated against Tap+Paymob TEST modes with evidence in the task tips; ruff+mypy+pytest+lint-imports green; commit."
    }
  ],
  "tasks": [
    {
      "id": "social-providers",
      "group": "G16",
      "title": "Google + Apple social login from env",
      "endGoal": "Settings-based SOCIALACCOUNT_PROVIDERS built from env creds with verified-email auto-connect; absent creds degrade gracefully in local (no crash, provider simply unavailable).",
      "tips": [
        "Apple's secret is a multiline PEM - pass it base64-encoded through a SecretStr env field and decode in settings.",
        "Must override the social adapter's save_user to call services.user_post_signup after the user is saved (no-signals rule; account adapter sees commit=False on this path).",
        "MOVED to end (user decision 2026-07-11): social login needs external Google/Apple OAuth credentials that do not exist yet - build it when they do. The prerequisites already in place: allauth[socialaccount] extra installed, headless mounts socialaccount endpoints automatically once the app is installed, skip-OTP method-list configured. Do this BEFORE the auth-investigation review so the review covers social."
      ],
      "status": "pending"
    },
    {
      "id": "auth-investigation",
      "group": "G16",
      "title": "USER REVIEW: deep-dive into the implemented authentication",
      "endGoal": "Omar has reviewed the IMPLEMENTED auth stack in detail and signed off; decisions/changes recorded here. Scope unchanged: passwordless email-code flow, browser vs X-Session-Token clients, HEADLESS_ONLY + frontend URLs + served auth spec, adapter hooks, Google/Apple social + auto-connect, usersessions, kill-switch, axes/ratelimit posture, optional secure_admin_login.",
      "dependsOn": ["social-providers"],
      "tips": [
        "USER INVESTIGATION (requested 2026-07-11): Omar leads this; agent supports with research/prototypes on request. G04 implementation must not be finalized before sign-off.",
        "References: allauth headless docs (openapi spec + client types), Gawdat_Django_Template config/helpers/account_adapter.py (6-digit codes, skip-OTP-for-social) and allauth_adapter.py (headless user payload), PLAN.md Auth row.",
        "Already decided (context): passwordless (no user passwords; staff keep Argon2), no JWT, no signals - adapters call services (signup hook = save_user; social adapter must trigger user_post_signup itself due to commit=False pass-through).",
        "MOVED to end (user decision 2026-07-11): no longer gates G04 - implementation proceeds on PLAN defaults; this became a sign-off review of working code (easier to evaluate than docs).",
        "G12 runtime finding (2026-07-12): allauth's login-code rate limit trips after 3 requests per email but surfaces as HTTP 400 with error code 'too_many_login_attempts' (localized - Arabic body observed), NOT 429 - that is allauth's by-design form-error path, in both browser and app clients. django-ratelimit's 429 handler applies to OUR endpoints once any are decorated."
      ],
      "status": "pending"
    },
    {
      "id": "factories-review",
      "group": "G17",
      "title": "USER REVIEW: factories topic (factory_boy + Faker)",
      "endGoal": "Omar has investigated factory_boy (DjangoModelFactory, Sequence, django_get_or_create, skip_postgeneration_save, post_generation) and Faker providers against the implemented factories + registry, and signed off or requested changes (recorded here).",
      "tips": [
        "IMPLEMENTED 2026-07-11 (user-approved design): factory_boy structure + mimesis values (Locale.AR_SA / EN via apps/common/tests/fake.py - the only value source). Review targets: apps/users/tests/factories.py (UserFactory: Sequence email get-or-create, weighted ar/en language, locale-matched name, password='!', verified-EmailAddress post_generation, staff Trait), apps/common/tests/factories_registry.py (explicit dict, loud LookupError), apps/common/tests/test_factory_coverage.py (gate), apps/common/management/commands/seed_db.py (--scale 0..1 log curve to 1M, build_batch + chunked bulk_create, fan_out(per_parent) for child ratios, @seed.example.com marker, --wipe/--seed, local-only guard).",
        "Runtime evidence: 10k users + 10k verified EmailAddresses in 1.0s (~21k rows/s => scale 1.0 ~ 2min); seeded user code-login pends on login_by_code (not verify_email) with Arabic code email in Mailpit; wipe removed exactly the seeded rows. CLAUDE.md 'Factories & seed data' section is the standing rule."
      ],
      "status": "pending"
    },
    {
      "id": "gateways-e2e-verify",
      "group": "G17",
      "title": "Verify payment + SMS gateway workflows end-to-end",
      "endGoal": "Both external-gateway flows observed working end-to-end, evidence recorded here: (1) payments - create payment -> gateway redirect/charge -> webhook callback -> payment status transition -> wallet applied, including a rejected invalid-HMAC webhook; (2) SMS - notification send -> routing selects provider by destination -> delivery task executes on the worker -> provider client called and delivery recorded on the Notification row.",
      "tips": [
        "Run the whole flow through the real seams, not mocked services: worker running (`just worker`), webhook hit over HTTP (curl or test client against the running app), state verified in DB/admin afterwards.",
        "Local tier uses the built-in doubles: gateways/fake.py for payments and the SMS backend equivalent - this proves the wiring (services -> tasks -> clients -> models) without external creds.",
        "Real-provider tier needs sandbox credentials (Paymob, Tap, OurSMS, SMSMisr) that may not exist yet - if absent, record that explicitly here and verify the fake tier only; do NOT let missing creds silently pass as verified (same convention as social-providers).",
        "Webhook security is a first-class case: 9018b26 made blank-secret HMAC fail closed - exercise both a valid-signature accept and an invalid/missing-signature reject.",
        "SMS routing: cover one Saudi number (-> oursms) and one Egyptian number (-> smsmisr) so clients/sms/routing.py is actually observed choosing.",
        "Idempotency: replay the same webhook twice - second delivery must not double-apply the wallet transaction."
      ],
      "status": "pending"
    },
    {
      "id": "cron-jobs-check",
      "group": "G18",
      "title": "Verify the scheduled-jobs story against django.tasks gaps",
      "endGoal": "The project has a demonstrated, documented pattern for recurring and delayed work: deadline-as-data (e.g. expires_at) + an idempotent sweep management command that goes through services; a cadence trigger per environment (local = manual/compose loop, prod = external scheduler); a cleanup command that prunes finished task-result rows.",
      "tips": [
        "django.tasks core (Django 6.0) provides API only - NO worker, NO retries, NO delayed execution (run_after absent from core), NO periodic tasks, NO delivery guarantees, NO monitoring dashboard. Each gap needs a named owner in this repo.",
        "Worker = the django-tasks reference package's DB backend + db_worker (`just worker`); in prod it is a separate long-running service deployed in lockstep with web so task code never drifts.",
        "Sweep pattern (decided 2026-07-18): never schedule one event per row - store the deadline on the model, filter reads on it so correctness never depends on the scheduler, and let a frequent idempotent sweep materialize the state. Side-effectful closes go row-by-row through the owning service, not bulk update().",
        "Sweep commands live in apps/<app>/management/commands (layering: management -> services); recurring cadence changes are infrastructure-only edits, new jobs = new command + new schedule entry.",
        "JSON-only task args (tuples become lists, no datetimes/models) - the existing pk-strings rule already complies; keep return values JSON-safe or return nothing.",
        "No timeouts / rate limits / uniqueness in the framework: guard in the task body (select_for_update, status checks) where duplicate or hung execution would hurt."
      ],
      "status": "pending"
    },
    {
      "id": "aws-deploy-runbook",
      "group": "G19",
      "title": "Write the AWS deployment runbook (ECS Express Mode + worker + schedules)",
      "endGoal": "docs/ contains a runbook covering: web tier via ECS Express Mode (ECR image, execution + infrastructure roles, containerPort, health-check path, env/secrets, task role for S3/SES), worker as a plain ECS service on the same cluster, EventBridge Scheduler entries kept in IaC for every cron job, and the explicit note that DB/Redis are outside Express Mode's scope.",
      "tips": [
        "Express Mode facts (researched 2026-07-18): HTTPS URL https://<name>.ecs.<region>.on.aws, ALB auto-shared across up to 25 services with same networking, canary deploys + 5XX rollback alarm (strategy not changeable), defaults 1 vCPU/2GB, target-tracking 60% CPU min 1/max 20 tasks, no extra charge - pay for Fargate/ALB/CloudWatch/transfer.",
        "Custom task definitions supported since July 2026: container must be named 'Main' with a single TCP port mapping and FARGATE compatibility; taskDefinitionArn is mutually exclusive with primaryContainer/cpu/memory/role params. Never rename the Main container - it breaks Express Mode updates.",
        "First Express service in a VPC pins the shared ALB's subnets/AZs - create with subnets from ALL AZs. Public subnets -> internet-facing ALB + public task IPs; private subnets -> internal ALB (bring your own NAT).",
        "Custom domain is not first-class: manual ACM cert + host-header listener rule on the shared ALB + Route 53 record.",
        "IaC: Terraform aws_ecs_express_gateway_service (provider >= 6.23.0); CloudFormation/CDK supported since launch. Schedules live in IaC next to the commands they trigger - never click-ops.",
        "Recurring jobs: EventBridge Scheduler -> ECS RunTask with command override `python manage.py <command>` reusing the task definition family; one-off Fargate seconds per run, no always-on beat process.",
        "AWS positions Express Mode as the migration target for App Runner (availability changing) and the EOL Copilot CLI - a point in favor of betting on it."
      ],
      "status": "pending"
    },
    {
      "id": "multi-country-multi-currency",
      "group": "G21",
      "title": "Handle multi-country and multi-currency support",
      "endGoal": "The platform supports operating in more than the current two markets: a new country/currency can be added by configuration + one gateway class, wallets and payments behave correctly across currencies, and every hardcoded two-currency assumption is either generalized or has a loud guard.",
      "tips": [
        "Requested by Omar 2026-07-18. Current state: Currency enum = SAR/EGP only (apps/payments/constants.py), one wallet per user fixed at signup in DEFAULT_CURRENCY=SAR, topups reject currency mismatch at initiate AND credit time.",
        "Known hardcoded spots from the 2026-07-18 payments review: _MINOR_UNIT_EXPONENT in gateways/base.py maps only SAR/EGP (=2) - a 3-decimal (KWD/BHD) or 0-decimal currency needs an entry; tap.py _format_amount hardcodes 2 decimals; PAYMENT_GATEWAYS needs an entry per currency (missing = clean 503).",
        "Design decisions to make and record here: one wallet per user vs wallet-per-currency; where country lives (User field? phone-derived? request locale?); FX/conversion explicitly in or out of scope; whether SMS routing (clients/sms/routing.py picks provider by phone country) generalizes with the same country registry.",
        "Arabic-first rule already covers locale; check mimesis locales in apps/common/tests/fake.py if new locales ship."
      ],
      "status": "pending"
    },
    {
      "id": "zones-from-dashboard",
      "group": "G21",
      "title": "Load zones from the dashboard",
      "endGoal": "Zones are data, not code: staff create/edit zones in the admin dashboard, the app reads them at runtime through selectors, and zone changes require no deploy.",
      "tips": [
        "Requested by Omar 2026-07-18. Scope to clarify with Omar before building: what a zone is (geographic service/delivery area? pricing region? country subdivision?) and which app owns it (likely a new bounded-context app per the one-app-one-domain rule).",
        "Follow the house patterns: model inherits BaseModel (uuidv7 pk) + factory registered in factories_registry (coverage gate), admin package scaffolded via `manage.py generate_dashboard <app> Zone`, writes through a zone service, reads through selectors.",
        "Zone names are user-facing: per-app translation.py (django-modeltranslation) for Arabic-first naming.",
        "If zones are geometric (polygons/radii) decide storage early: PostGIS/GeoDjango is a real infra dependency (RDS supports it) vs simple structured fields (city/district lists) - record the decision here."
      ],
      "status": "pending"
    },
    {
      "id": "claude-structure-audit",
      "group": "G20",
      "title": "Audit CLAUDE.md digest + .claude/rules structure",
      "endGoal": "Every rule with depth has a topic file in .claude/rules that auto-loads on the right file globs; the CLAUDE.md digest is a faithful one-screen summary of those files; no contradiction between CLAUDE.md, PLAN.md, and docs/ARCHITECTURE.md.",
      "tips": [
        "The cross-app access rule was added to the digest 2026-07-18 (one-way imports recorded in pyproject ignore_imports; passive model ownership only; behavior goes through the owning app's service; FKs as string refs) - check whether it deserves its own .claude/rules topic file like factories.md has.",
        "Inventory pass: list .claude/rules/*, map each to its digest line and its trigger globs; flag digest rules with no rule file and rule files with no digest line, then decide each case deliberately rather than mechanically adding files."
      ],
      "status": "pending"
    },
    {
      "id": "saved-cards-live-verify",
      "group": "G22",
      "title": "Prove saved-card flows against live test-mode gateways",
      "endGoal": "With real Tap/Paymob TEST keys and a webhook tunnel: Tap checkout with save_card stores card_/cus_/payment_agreement_ ids from the webhook, one-click (tokens->charge, customer_initiated true) and `manage.py charge_saved_card` (MIT, non-3DS) both settle CAPTURED, and DELETE /cards removes the card at Tap; Paymob's hosted Save-Card checkbox delivers a TOKEN callback whose 8-field HMAC verifies and stores the token, a CIT intention with card_tokens shows the saved card CVV-only, and a MOTO intention + payments/pay charges with no customer present; a forged TOKEN hmac is observed rejected as 400.",
      "tips": [
        "Both provider accounts had Save Card / tokenization enabled 2026-07-18 (user confirmed). Paymob live mode needs PAYMOB_COF_INTEGRATION_ID + PAYMOB_MOTO_INTEGRATION_ID in .env; in test mode leave CoF empty (falls back to PAYMOB_INTEGRATION_IDS) and ask Paymob support for a test MOTO id if MIT is to be proven there.",
        "Reuse the gateways-e2e-verify tunnel setup (BACKEND_BASE_URL = ngrok URL); the TOKEN callback arrives as a SECOND POST after the transaction one - watch the webhook log for both.",
        "mada test cards are the ones that matter for Tap MIT in KSA - verify at least one mada and one Visa/MC saved-card charge.",
        "TAP HALF VERIFIED 2026-07-18 against sandbox (sk_test key, MC test card 5123...0008, ngrok tunnel): (1) save - charge with save_card:true, user paid + 3DS on hosted page, webhook delivered card_TS90A58261723QDmR18Kd6j321 / cus_TS06A1220261724Ja941807818 / payment_agreement_AZUG12261724iJM818qY6D847, SavedCard row created (expiry NOT returned in the webhook card object - exp fields stayed null, list-cards would have them); (2) MIT charge_saved_card 5 SAR - tokens+charges both 200, CAPTURED synchronously, no 3DS; (3) one-click CIT 7 SAR - charge with token source returned INITIATED + a tap_process.aspx 3DS URL (structural checkout_url path confirmed), user approved, webhook flipped it to paid; (4) delete - DELETE /v2/card 200 and Tap list-cards then returned 0 for the customer. Remaining for this task: the Paymob half + a mada-card MIT.",
        "Sandbox quirk seen: the one-click charge's 3DS challenge URL is acceptance.sandbox.tap.company (not checkout.tap.company) - it is the challenge page, resolves after OTP approval, webhook follows within seconds.",
        "PAYMOB PARTIALLY VERIFIED 2026-07-18 (egy_sk_test key, merchant 1014916, ngrok tunnel): EGP payment_initiate routed to PaymobGateway with zero code changes, intention 201, user paid MC test card 5123...2346 on unified checkout, TRANSACTION callback 20-field HMAC verified against the real secret and flipped the payment to paid (txn 498359199, Approved). BLOCKED: no Save-Card checkbox renders - card tokenization is not provisioned on the TEST card integration 4912488 (live has a SUBSCRIPTION integration 5464491, so the feature exists account-side). Paymob's official 'Pay with saved card' Postman collection proves the tokenize intention carries NO special field (extras is a dummy) - the checkbox is purely integration config, so this is a support ask, not code. Ask Paymob: enable card tokenization on TEST integration 4912488 + create a TEST MOTO integration (for MIT). Also: the integration ids the user first supplied (4914544/4912532) do not exist on the account - real TEST ids are 4912488 (card VPC) / 4912492 (wallet UIG), listed via /api/ecommerce/integrations.",
        "Paymob retest procedure once support enables tokenization (~5 min): ngrok up, set BACKEND_BASE_URL + ALLOWED_HOSTS in .env, runserver, scratchpad paymob_live_1_save.py creates the save-card checkout (user pays + MUST tick the checkbox), watch for the SECOND (TOKEN) webhook storing the SavedCard by billing-email linkage, then one-click CIT via payment_initiate(saved_card=...) and DELETE /cards; MIT additionally needs PAYMOB_MOTO_INTEGRATION_ID in .env.",
        "PAYMOB SAVE VERIFIED LIVE 2026-07-19 (after the user enabled the save-card option on the account): the Save-Card checkbox rendered on unified checkout, 60 EGP paid (txn 498504017), and the TOKEN callback arrived as the second POST - its 8-field HMAC verified against the real secret and email linkage stored MasterCard **** 2346 for the paying user (both webhooks 200). CIT also verified visually: an intention with card_tokens rendered the saved card CVV-only + '+ New card' on the hosted page (normal-3DS fallback ids - the Wallet tab shows until a dedicated CoF id narrows payment_methods). NOT yet observed: the CIT settle webhook (three checkout sessions expired ~1h before the user completed CVV+Pay; retest = one-click link, CVV 123, watch webhook) and MIT (still needs the MOTO test integration id).",
        "Current Paymob docs verified 2026-07-18 (developers.paymob.com/paymob-docs, pages dated June 2026; fetch .md URLs with a browser UA, WebFetch gets 403): (a) token-callback HMAC is STILL the 8 fields we implement - the newer callback fields user_added / next_payment_intention are NOT signed; (b) the TOKEN callback is delivered to the intention's notification_url (or the integration's dashboard callback) - our per-intention URL works, no dashboard config needed; (c) create-card-token accepts THREE card integration types: Verification / Normal 3DS / Auth - a VERIFICATION integration validates the card with zero funds moved, i.e. Paymob's save-card-WITHOUT-payment (Tap equivalent: POST /v2/card/verify, probed working on sandbox 2026-07-18 - vry_ object, 3DS url, no amount). Refined support ask for merchant 1014916 TEST mode: enable Save-Card checkbox/tokenization on 4912488 + create TEST integrations for Verification (add-card-no-payment), MOTO (MIT), and Card-on-File (CIT); wire ids into PAYMOB_*_INTEGRATION_ID env fields (Verification would need a new optional field if adopted)."
      ],
      "dependsOn": ["gateways-e2e-verify"],
      "status": "pending"
    }
  ]
}
