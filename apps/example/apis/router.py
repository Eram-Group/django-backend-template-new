"""Aggregates the app's per-entity routers into one mount point."""

# from ninja import Router
#
# from apps.example.apis.examples import router as examples_router
#
# router = Router()
# router.add_router("", examples_router)
