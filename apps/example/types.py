"""Value objects, dataclasses, TypedDicts, type aliases - importable by every layer.

Single-operation policy does NOT belong here - keep it next to its operation
(see USER_UPDATABLE_FIELDS in apps/users/services/users.py).
"""
