# Backend

API-only Django backend scaffold (work in progress).

The design lives in [PLAN.md](PLAN.md); the build sequence in [TODO.json](TODO.json).
A full README (quickstart, commands, AWS runbook) lands at the end of the transformation.
